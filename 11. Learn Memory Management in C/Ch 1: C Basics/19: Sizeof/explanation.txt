sizeof is a unary operator that allows us to see how much memory something takes


    bool
    int
    float
    double
    size_t

size_t is a special typedef type which takes the maximum non struct allowed portion of memory for a variable by architecture

size_t is part of stdint library
